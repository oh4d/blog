apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "magazine.fullname" . }}-test-connection"
  labels:
{{ include "magazine.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "magazine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
