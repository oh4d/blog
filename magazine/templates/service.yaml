apiVersion: v1
kind: Service
metadata:
  name: {{ include "magazine.fullname" . }}
  labels:
{{ include "magazine.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "magazine.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
